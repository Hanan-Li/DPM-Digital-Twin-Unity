using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class offff : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject robot;
    public GameObject stupid;
    void Start()
    {
        //Debug.Log($"distance from robot:{(robot.transform.position - transform.position).x}");
        //Debug.Log($"distance offset:{(stupid.transform.position - robot.transform.position).x}");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
